import time

from imutils import paths
import A_Final_Sys.oldcare.utils.getfromURL as URLTool

# dataset_path = '../images'
# image_paths = list(paths.list_images(dataset_path))
# image_path = image_paths[0]
# URLTool.upload_img(image_path)

